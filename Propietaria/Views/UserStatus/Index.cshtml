@model IEnumerable<Propietaria.Models.UserStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Estados de Usuario</h2>

<p>
    @Html.ActionLink("Agregar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Nombre
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdUserStatus }) |
            @Html.ActionLink("Ver", "Details", new { id=item.IdUserStatus }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdUserStatus })
        </td>
    </tr>
}

</table>
