@model IEnumerable<Propietaria.Models.ReclaimsAndComplaintsStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Estados de Quejas y Reclamos</h2>

<p>
    @Html.ActionLink("Agregar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Nombre
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdReclaimsAndComplaintsStatus }) |
            @Html.ActionLink("Ver", "Details", new { id=item.IdReclaimsAndComplaintsStatus }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdReclaimsAndComplaintsStatus })
        </td>
    </tr>
}

</table>
